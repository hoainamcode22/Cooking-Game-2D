using UnityEngine;

public class HHDecorMarketStall : MonoBehaviour
{
    private void OnTriggerEnter2D(Collider2D other)
    {
        // Decor-only copy: keep the component reference valid without opening HappyHarvest UI.
    }
}
