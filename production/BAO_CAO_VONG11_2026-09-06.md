# BÁO CÁO VÒNG 11 - 06/09/2026 — PLOT / ID / UI PLACE / HUD

> 4 Dev + Lead. **7 file `.cs`, 1 `.asset`, 1 scene, 2 sprite mới vào Resources.**
> Backup: `production/backup_vong11_2026-09-06/` (gồm `lead/`).
> **Lead CHECK: 7/7 file `.cs` ĐẠT** · scene 7219 block · 0 `m_Component` mồ côi · asset header nguyên.

---

## 1. BUG P0 "MUA 1 Ô MẤT 1 PLOT" — GIẢ THUYẾT CỦA LEAD SAI, DEV X TÌM RA GỐC THẬT

**Lead nghi `houseId = itemData.name` trùng khoá. Dev X BÁC BỎ bằng bằng chứng:**
`HouseGrowthController.GetSaveKey()` là `HouseSave_{houseId}_{x}_{y}` — **có toạ độ trong khoá** nên hai nhà
cùng loại không thể chung khoá. Và `Plot_01.prefab` **không hề có `HouseGrowthController`**
(component list chỉ có `EditableBuilding`, `PlotController`, `BuildingFootprintKit`, `PlotCropVisual`).
Dòng 1587 Lead nghi **không thể** là nguyên nhân.

### Gốc lỗi 1 (làm plot BIẾN MẤT): `DisablePlaceholderInScene`
`PlacementManager.cs:1677 go.SetActive(false)` — hàm này tắt **mọi object trong scene có tên TRÙNG KHÍT tên
prefab** đang đặt (`:1671 if (go.name != prefabName) continue;`).

Dev X dò thẳng `SCN_Farm.unity` và tìm ra object **THẬT, ĐANG BẬT, mang đúng tên prefab của món trong Shop**:

| Object trong scene | Dòng | plotId | Bị món nào giết |
|---|---|---|---|
| `Plot_01` | 456544 | **106** | `Đất.asset` (prefabToBuild.name = "Plot_01") |
| `Chauhoa_1` | 422052 | **21** | `Chậu Hoa1.asset` |
| `Pen_03` | 420213 | | `Chuồng Gà.asset` |
| `House_01` | 4581 | | `Home1.asset` |

⇒ **Mua 1 ô đất là ô 106 bị tắt. Mua 1 chậu hoa là chậu 21 bị tắt.** Đúng nguyên văn Sếp báo.
Tệ hơn: `LoadBuildings():1562` cũng gọi hàm đó ⇒ **lỗi tái diễn MỖI LẦN mở game**.

Bằng chứng vì sao chỉ nổ bây giờ: **25 ô/chậu dạng GameObject thường trong scene đang `m_IsActive: 0`**,
ruộng thật chỉ còn 13 PrefabInstance, và 2 trong 13 cái đó mang đúng tên prefab của Shop.

### Gốc lỗi 2 (độc lập, làm ô cũ MẤT SẠCH CÂY): sai thứ tự
`PlacementManager.cs:1323-1325` gọi `plot.InitializeAsNew()` **TRƯỚC** `plot.SetPlotId()`.
`InitializeAsNew()` chạy `PlayerPrefs.DeleteKey(SaveKey)` với `plotId` **mặc định của prefab**:

| Món Shop | plotId mặc định prefab | Khoá bị xoá oan | Nạn nhân |
|---|---|---|---|
| Chậu Hoa1 | **21** | `PLOT_NORMAL_21` | `Chauhoa_1` ĐANG BẬT |
| Chậu Hoa2 | **22** | `PLOT_NORMAL_22` | `Chauhoa_1 (1)` ĐANG BẬT |
| Chậu Hoa3 | **24** | `PLOT_NORMAL_24` | `Chauhoa_1 (3)` ĐANG BẬT |
| Chậu Hoa4 | **23** | `PLOT_NORMAL_23` | `Chauhoa_1 (2)` ĐANG BẬT |

**4/4 loại chậu đều bắn trúng một chậu thật.** Chậu 21 hứng **cả hai** gốc lỗi.

### "Không click trồng được" = CÙNG GỐC, không phải lỗi riêng
`SetActive(false)` tắt cả object ⇒ Collider2D tắt ⇒ `Physics2DRaycaster` không bắn trúng ⇒
`PlotController.OnPointerClick` không chạy, `PlantDragController` cũng không thấy.
**Một câu của Sếp, một nguyên nhân.**

### Đã sửa (4 hunk, `PlacementManager.cs`)
Đảo thứ tự `SetPlotId()` TRƯỚC `InitializeAsNew()` ở cả 2 đường (`ConfirmPlacement` + `RegisterCompletedBuilding`);
`DisablePlaceholderInScene` **bỏ qua mọi object có `PlotController`**; và `GetNextPlotId()` dùng **bộ đếm bền**
`FARM_NEXT_PLOT_ID` với `Mathf.Max(stored, maxId + 1)`.

**Không phải wipe save.** `Mathf.Max` tự mồi từ scene nên người chơi chưa có khoá đếm vẫn đúng.
`SetActive(false)` chỉ xảy ra runtime, không ghi vào file scene ⇒ ô 106 và chậu 21 **tự sống lại** lần Play sau.

---

## 2. ĐỊNH DANH DUY NHẤT (yêu cầu "công trình nào cũng vậy")

Dev X chốt **số nguyên tăng dần, lưu bền, không tái sử dụng**, không dùng GUID vì `PLOT_NORMAL_{id}` và
`SavePlotSnapshot.plotId` đang là `int` — đổi sang GUID là phá tương thích save của mọi người chơi.
Mẫu noi theo có sẵn trong dự án: `DecorGrowthBootstrap.AllocateSlotIndex`.

**Đã làm cho ô đất và chậu.** Còn 3 hệ Dev X phát hiện **đang trùng khoá thật**, chờ điều phối:

| Hệ | Khoá | Vấn đề |
|---|---|---|
| **Chuồng** | `PenState_/PenFood_/PenStartTime_{penId}` | `penId` nằm trong `PenMiniPanelConfig` là **ScriptableObject DÙNG CHUNG** ⇒ **mua chuồng gà thứ hai là hai chuồng chung một khoá save**: cho ăn con này thì con kia cũng đang chế biến. **Lỗi mất dữ liệu thật, chỉ chờ Sếp mua chuồng thứ hai là nổ.** |
| **Máy xay** | `MILL_S{i}_Recipe/_EndTicks/_TotalSec` | **toàn cục**, không có id máy ⇒ mua 2 máy dùng chung một bộ slot |
| **Nhà dân** | `HouseSave_{houseId}_{x}_{y}` | khoá chứa toạ độ ⇒ **kéo nhà đang xây sang chỗ khác thì nhà xây xong MIỄN PHÍ** |
| Decor | `DecorGrow_{itemID}_{slot}` | **ĐÚNG rồi**, dùng làm mẫu |

---

## 3. BỎ QUY TRÌNH XÂY PLOT (Dev Y)

`Đất.asset` có `buildTimeSeconds: 30` nên ô đất đi **đường công trường 30 giây**. Chậu (`0`) và chuồng đã đặt ngay.

**Ai dựng 4 thứ trong ảnh Sếp:** banner "ĐẤT TRỒNG" = `ConstructionSiteUI.Construct()` dòng 104/117 ·
thanh giá = dòng 129/137/146 · nút gem = dòng 152/163/178/187 · **2 công nhân** = `BuildWorkers()` dòng 184,
đúng 2 người vì dòng 207 `workerCount = w >= 300f ? 2 : 1` mà ô 1x1 iso rộng **chính xác 300**.

**Đã bỏ cho ĐÚNG nhóm ô đất và chậu, GIỮ cho công trình lớn** (Sếp chỉ nói "plot", không nói bỏ cả hệ).
Điều kiện đọc từ data qua `DecorGrowthConfig.IsPlotObject` (nhận theo **component `PlotController` trên prefab**,
nên ô đất mới do designer thêm sau tự được nhận) + `excludedItemIDs` (100/109/110/111/112).
Cờ `skipConstructionForPlots = true` để Sếp bật lại.

Dev Y **cố ý không dùng `ShouldApply`** vì hàm đó chốt ở cờ `enabled` của hệ 5-stage — tắt hệ đó là không
phân biệt được ô đất với cái máy.

> ⚠️ **BỊT LUÔN MỘT LỖ SẾP CHƯA BIẾT:** trong 30 giây xây, `placedBuildings` **chưa có entry**, mà
> `PlotPurchasePricing.EffectiveGoldPrice` đếm qua `CountPlacedByItemId` ⇒ **bấm mua liên tục trong 30 giây
> thì mọi ô đều tính giá của ô đầu tiên, giá luỹ tiến bị vượt.** Bỏ công trường làm cửa sổ đó thành 0.
> Trả lời trực tiếp câu Sếp hỏi "mua bao nhiêu tùy ý mỗi lần là có logic riêng hết chứ": **TRƯỚC ĐÂY LÀ CHƯA.**

**Save cũ đang treo công trường ô đất:** hoàn thành ngay lúc load, **không hoàn tiền** — vì `SiteEntry`
không lưu giá đã trả, mà ô đất là giá luỹ tiến, tra lại sẽ ra giá ô KẾ TIẾP. **Không mất ô của ai.**
Không bump `SaveVersion` (bump là mất luôn công trường nhà và máy 240-900 giây của người chơi).

---

## 4. UI CARD PLACE (Dev AA) — "gắn assets vào đừng chỉ dựng khung nền"

**Sếp nói đúng: vòng 10 Dev V dựng card bằng SPRITE VẼ THỦ TỤC.** Vòng 11 thay bằng art thật:

| Bộ phận | Vòng 10 (vẽ code) | Vòng 11 (art thật) |
|---|---|---|
| Khung ngoài card | `TheBoGocCoVien()` | `shop_card_outer` |
| Nền giấy | `TheBoGocCoVien()` | `shop_card_inner` |
| Nút ✓ | `NutVuongNoi()` | `check_badge_green` (đĩa tròn) |
| Nút ✗ và 🗑 | `NutTronNoi()` | `btn_red_small` 9-slice |
| Nút ↻ | `NutTronNoi()` | `btn_yellow_3d` |
| Ruy băng giá | không có | `ribbon_banner_gold` (thêm mới) |
| Nền thanh giờ | `Panel()` | `timer_box_dark` |
| Nền nút rush | `GreenButton()` | `btn_green_3d` |

3 hàm của Dev V **không xoá dòng nào**, tụt xuống hàng dự phòng khi `Load()` trả null.

**Nguyên nhân "xấu quá" tìm ra được:** mọi sprite thật **đã bake màu vào texture**. Vòng 10 tô
`mauVienThe (0.42,0.27,0.14)` lên art nâu ra `(77,34,9)` **màu bùn**, và tô `confirmButtonColor` lên đĩa xanh
ra `(30,152,14)` **xanh sẫm đục**. Vòng 11 để tint TRẮNG. Kiểu vẽ giờ **suy từ chính sprite**
(`border != 0` ⇒ Sliced), nên Sếp thay art khác cỡ là code tự đúng.

**Thon gọn:** card **nhỏ hơn 43%** (403x217 px → 282x176 px). Nút nhỏ nhất **92.2 px**, vượt sàn 88 px cho
đầu ngón tay ở **mọi mức zoom VÀ mọi chiều cao màn**.
> Dev AA còn phát hiện **vòng 10 hỏng trên màn 720p mà không ai biết**: nút chỉ còn 73 px. Nguyên nhân:
> vòng 10 chỉ bù zoom nên px tỉ lệ thuận `Screen.height`. Nay nhân thêm bù DPI `clamp(1080/height, 1.0, 1.6)`
> ⇒ hệ số px/world thành hằng số. **Màn 1080 trở lên chạy y nguyên, không lệch một pixel.**

### "Không bám sát công trình" — BA nguyên nhân độc lập
1. **Nhánh LẬT SANG CẠNH nổ liên tục.** Mép dưới vùng ô phải cao hơn 313 px so với đáy màn mới không nổ, mà
   nửa dưới màn là vùng đất chính. Khi nổ, card bị đẩy **344 px sang bên**. Đã chốt `luonLatSangCanh = false`,
   card **luôn đứng trên trục X của công trình**, thiếu chỗ thì trườn LÊN theo Y.
2. **Trễ một frame.** `ProjectSettings` **không có `MonoManager.asset`** ⇒ thứ tự Update giữa
   `PlacementManager` và controller là **không xác định**. Ghost snap trọn một ô 300x150 nên trễ 1 frame là
   lệch cả một ô. Đã chuyển sang `LateUpdate()`.
3. **Hai nguồn sự thật khác nhau.** Card đọc ảnh chụp cũ `_tamVungOLocal` (ghi **một lần**, không cập nhật
   trong lúc kéo), còn 4 chevron đọc `pm.CurrentRect` mỗi frame ⇒ khung 4 góc ôm đúng mà card thì không.
   Đã cho card đọc **cùng nguồn** với chevron.
   Kiểm bằng đại số hình học iso, không đoán: 1x1 ra 300x150, 2x2 ra 600x300.

Cộng 2 lỗi phụ: `nuaRongVungO` dùng `lossyScale.y` cho bề RỘNG (đã sửa `.x`), và `hangGiaY` **sai hệ toạ độ**
⇒ hàng giá **tràn ra ngoài đỉnh card 23 px**, đó là lý do chữ dính sát mép giấy.

### Chữ đè trong `ConstructionSiteUI` (bảng số)
| | Trước | Sau |
|---|---|---|
| Thanh giờ: icon vs chữ | **CHỒNG 8 px** | **HỞ 20 px** |
| Nút rush: icon vs chữ | **CHỒNG 9 px** (Sếp chưa nêu, Dev AA tự tìm) | **HỞ 18 px** |

### Nút ✓ trông "mờ như hỏng"
Hai phần. **(A) Lỗi trình bày, đã sửa:** tô màu lên sprite có độ sáng bake sẵn ra xanh sẫm đục, mắt đọc ra
"đang tắt" **dù `interactable = true`**. **(B) Khi tắt thật:** prefab serialize
`m_DisabledColor` alpha **0.502** ⇒ nhìn xuyên thấy cỏ qua nút. Đã ghi `disabledColor` đục hẳn, nâng alpha
glyph 0.45 → 0.90, và thêm **kênh thứ ba**: tờ giấy chuyển hồng đất khi không đặt được (nút nói "chưa bấm
được", tờ giấy nói "vì CHỖ NÀY không được").

> **Cách Sếp tự phân biệt trong 10 giây:** nhìn **4 chevron ở 4 góc vùng ô**. Chevron **XANH** mà ✓ vẫn mờ
> ⇒ lỗi trình bày (đã sửa). Chevron **ĐỎ** ⇒ code thật sự cho rằng không đặt được, bật `logPlacementReason`
> để thấy tên cổng chặn.

---

## 5. LEAD TỰ LÀM

| # | Việc | Kiểm |
|---|---|---|
| 1 | **`ConstructionArtKit`: 3 ô gắn SAI sprite → None.** `namePlateBg` và `timerBarBg` đang trỏ **`Sprite_clock_icon.png` 94x89 border 0** (ICON ĐỒNG HỒ!) bị kéo giãn thành nền 404x76 và 288x68. `rushButtonBg` trỏ **đống tiền vàng 256x256** bị bóp thành 224x84 | header `%YAML` nguyên |
| 2 | **Tắt `Tilemap_IsoFence` renderer** (`m_Enabled: 1 → 0`). **Giữ nguyên 106 tile** nên hoàn tác được bằng 1 tick | 7219 block không đổi |
| 3 | **8 số HUD của Dev W** (bóp đuôi capsule rỗng + dịch icon và chữ) | 0 `m_Component` mồ côi |
| 4 | **2 field `RewardFlyFX.targetGold/targetGem`** từ `{fileID: 0}` → trỏ đúng 2 capsule. **BẮT BUỘC**, không gán thì punch tụt xuống 1.15 | |
| 5 | **Copy `ob_check.png` + `ob_trash.png` vào `Resources/UI/Standard/`** với **guid MỚI** (trùng guid là Unity báo lỗi), và thêm `PathObCheck`/`PathObTrash` vào `UIStandardSprites.AllPaths` ⇒ dấu ✓ và 🗑 art thật **chạy được trong build**, không còn rơi về dấu vẽ code | brace cân, AllPaths xác nhận |

### Khung coin sát khung gem (Dev W)
Yêu cầu của Sếp **ngược với ràng buộc** Dev W đã tìm ra: khe gold-right tới icon-gem-left chỉ còn **7.5 px**
khi cả hai nảy, và đó là **khe hẹp nhất toàn HUD**. Nên Lead giao đúng **đòn bẩy Dev W đã đề xuất mà chưa dùng**:
bóp **phần capsule RỖNG** ở đuôi (đo thật: Vàng **83.4 px**, Gem **69.6 px** rỗng sau khi ẩn 2 nút cộng).

Dev W đo bề rộng chữ bằng **metrics font thật** (`Baloo2 SDF`, cỡ 26) chứ không đoán: `"9.999.999"` = 110.77 local.

**Kết quả: khoảng trống nhìn thấy 102.6 px → 78.6 px (giảm 24.0 px, 23.4%)**, mà:
khe an toàn **giữ đúng 7.5 px** (không tệ hơn vòng 10) · punch vàng **1.215**, gem **1.202** (đều ≥ 1.20) ·
mép phải capsule Gem **giữ nguyên 1775.8** nên khoảng cách tới bánh xe cài đặt không đổi một pixel.
Bonus: mép trái cụm phải lùi thêm **150 px** khỏi giữa màn, và **4:3 (iPad) từ 6.7 px lên 127.4 px**.

**Giới hạn cứng: 77.8 px.** Phương án này đạt 78.6, **cách sàn 0.8 px**. Không nhồi thêm được.
Muốn thấp hơn phải bỏ một trong bốn thứ Sếp/Lead đã duyệt (bảng giá ở báo cáo Dev W).

> ⚠️ **Hai nút cộng không thể bật lại nữa.** Đoạn đuôi vừa cắt chính là chỗ chúng nằm. Bỏ tick `anNutCong`
> thì `Btn_Add_Gold` thò 28 local ra ngoài capsule và đè lên số.

---

## 6. 🧑 SẾP LÀM

**Bước 1 — Đóng Unity rồi mở lại.** Nó đang giữ scene trong RAM mà Lead vừa sửa. **Hỏi lưu thì chọn `Don't Save`.**
Có 2 sprite mới trong `Resources/UI/Standard/` nên Unity sẽ import thêm.

**Bước 2 — Test bug P0 (quan trọng nhất):**
- Ô `Plot_01` (plotId 106) và chậu `Chauhoa_1` (plotId 21) phải **còn nguyên** sau khi mua ô mới.
- Mua liên tiếp 3 ô đất **thật nhanh**: giá phải tăng **50, 70, 90** chứ không phải 50, 50, 50.
- Ô mới đặt xuống phải **click trồng được ngay**, không qua công trường, không banner, không 2 công nhân.
- Mua 1 cái **Máy**: VẪN phải có công trường 240 giây đầy đủ (để chắc là không bỏ cả hệ).

**Bước 3 — Test UI card:** card phải là **thẻ giấy kem khung gỗ art thật**, nhỏ gọn hơn rõ rệt,
**bám sát công trình** không trôi sang bên, nút ✓ đĩa xanh tươi có **dấu ✓ art thật**, nút ✗ vuông đỏ.
Zoom ra zoom vào nút không teo dưới 92 px.

**Bước 4 — Nhìn HUD:** khung vàng và kim cương sát nhau hơn 24 px, mép phải khung gem không đổi.

---

## 7. ⚠️ CÒN TREO
1. **"Plot khít vào ô lưới"** — Lead CHƯA giao Dev riêng. Lý do: vòng 10 Dev U đã đổi ô lưới về **300x150**
   và `gridSize` về **1x1**, và ảnh Sếp gửi có thể chụp TRƯỚC khi Unity nạp lại code đó.
   **Xin Sếp test lại sau Bước 1 rồi cho biết còn lệch không**, để không làm lại việc đã xong.
2. **3 hệ trùng khoá save** (chuồng, máy xay, nhà dân) ở mục 2 — cần điều phối, `PenMiniPanelUI` là gấp nhất.
3. **`Pen_03` và `House_01` vẫn bị `SetActive(false)`** khi mua chuồng gà / nhà dân. Dev X **cố ý không sửa**
   vì comment `:1647-1657` nói việc tắt placeholder nhà dân là **có chủ ý**. **CẦN SẾP QUYẾT:** mua "Chuồng Gà"
   thì con chuồng có sẵn trong scene biến mất (hiện tại), hay giữ cả hai?
4. **Ba nguồn "cái gì là ô đất"** đang song song (`PlotComponentNames`, `excludedItemIDs`, `PlotItemIds`
   hard-code). Thêm loại ô đất mới mà không sửa cả ba là lệch.
5. `btn_close.png` (đĩa đỏ **có sẵn dấu X**) vẫn chưa vào Resources — thêm thì bỏ được dấu ✗ vẽ thủ tục.
6. Tên asset đảo: `Chậu Hoa3.asset` trỏ `Chauhoa_4.prefab`, `Chậu Hoa4.asset` trỏ `Chauhoa_3.prefab`.
7. `remove_debug_logs.ps1` ghi lại bằng `File::WriteAllLines` (CRLF) ⇒ chạy nó là `PlacementManager.cs`
   (đang LF thuần) bị đổi hết line ending, `git diff` nổ ra toàn file.
